extends _includes/layouts/base.pug
include _includes/molecules.pug

block hero
    +container()
        +h1("Hello from Seattle! 🌲")
        +subheading("I’m a software engineer turned designer applying to design MFA programs.")
        br
        +subheading("Keep scrolling to check out my portfolio ⬇️")

block content
    //- TODO: this double container thing is really strange
    +container()
        +h2("Projects", true)
    +sectionWhite()
        +container()
            +projectListing("BREAK a Journaling Framework for Overcoming Limiting Beliefs", "photo url", "summary")
            +projectListing("Daily Lyric Posters", "photo url", "summary")
            +projectListing("Inclüsion T-shirt Design", "photo url", "summary")