// TODO: this can probably go away
// 
// var isLocal = location.hostname === "localhost" || location.hostname === "127.0.0.1" || location.hostname === "192.168.0.127:8080";
// if (!isLocal && window.location.protocol === "http:") {
//     window.location.protocol = "https:";
// }