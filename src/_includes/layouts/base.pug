include ../atoms.pug
include ../organisms.pug
include ../type.pug

//- TODO: this tag logic should probably not be in the base template
if tags
    - if (tags.includes("project"))
        - pageTitle = title + " – Design Portfolio – Shakeel Mohamed";
    - //else if (tags.includes("blog"))
        - //pageTitle = title + " by Shakeel Mohamed";
if title
    - pageTitle = title
else
    - var pageTitle = "Shakeel Mohamed – Designer & Brand Strategist";

doctype html
html(lang="en")
    head
        title #{pageTitle}

        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="chrome=1")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        meta(name="description" content=description)
        //- opengraph
        //- TODO: dynamic og:url with full permalink
        //- meta(property="og:url" content=page.url)
        meta(property="og:title" content=pageTitle)
        //- meta(property="og:image" content=pageImg)
        meta(property="og:description" content=description)
        meta(property="twitter:card" content="summary_large_image")
        meta(property="twitter:creator" content="@_Shakeel")
        
        //- TODO: canonical link

        link(rel="stylesheet" href="https://use.typekit.net/uun0nut.css")
        link(href="/dist/tailwind.css" rel="stylesheet")
        //- link(rel="preconnect" href="https://fonts.googleapis.com")
        //- link(rel="preconnect" href="https://fonts.gstatic.com" crossorigin)
        
        //- TODO: open graph via block?
        //- see https://iamjoona.com/blog/seo-tags-in-eleventy/
        //- TODO: favicon(s)
        //- TODO: sitemap
    //- TODO: remove debug-screens for production
    body(class="bg-navy debug-screens")
        +container()
            +header()
        main
            +container()
                block hero
            block content
        +footer()